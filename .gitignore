# Rust / Soroban
target
.soroban

# Stellar local config
.config

# Tests and snapshots
test_snapshots
playwright-report/
test-results/
src/test/e2e/__screenshots__/

# Frontend build output
dist
dist-ssr

# Server build output
server/build

# Dependencies
node_modules/
server/node_modules/

# Package manager state
.yarn/install-state.gz
.pnp.*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment files
.env
.env.*
!.env.example

# Local-only files
*.local.*

# Editor / OS artifacts
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Dev container — prevent env files from being tracked in git
.devcontainer/.env*

# Claude / AI tooling
.codex
.claude/

# Local-network deploy manifests (testnet manifests are committed)
deployments/local.json

# Generated contract clients
packages/*
!packages/.gitkeep
!src/contracts/util.ts
# Hand-written server SDKs live under packages/ too — keep their sources tracked
!packages/server-sdk/
!packages/server-sdk-python/
!packages/server-sdk-go/
!packages/server-sdk-rust/
.omx/

# Load-test secrets and generated results
load-tests/fixtures/generated.json
load-results/

# Generated security / gas reports (committed baselines stay tracked)
reports/
contracts/prompt-hash/gas-report.json
